---
- name: Local Tests.
  hosts: local
  gather_facts: true
  become: true
  vars:
    docker_compose:
      name: "test"
      image: "registry.micimo.de/docker/test"
      version: "1"
      debug: true
    docker_compose_builds:
      - from: "debian:trixie-20251208-slim"
  roles:
    - role: "../ansible"
